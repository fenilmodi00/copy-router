# build.io / Heroku-compatible Docker deploy from GitHub.
# Dashboard: Settings → Stack → dockerfile (or container), then Deploy → Connect repo.
build:
  docker:
    web: Dockerfile
  config:
    TARGETARCH: amd64
    PUBSUB_DISABLED: "true"
    SERVER_REPLICAS: "1"
