---
description: Alias for /unforce-model — clear an active /force-model pin and resume automatic routing.
---

/unforce-model

> `model="auto"` routes normally but never clears an existing pin — this
> command (or `/unforce-model`) is the only way to release one.
