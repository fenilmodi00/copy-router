---
description: Pin this session to a specific model via the Weave Router.
argument-hint: <model-id>
---

/force-model $ARGUMENTS

> Headless clients that can't type a slash command can pin the same way with
> the OpenAI-compatible `model` field — e.g. `model="moonshotai/kimi-k2.7":high`
> — it is treated as identical routing intent. The commands above stay valid.
