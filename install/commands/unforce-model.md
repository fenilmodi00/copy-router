---
description: Clear an active /force-model pin and resume automatic routing.
---

/unforce-model

> `model="auto"` routes normally but never clears an existing pin — this
> command (or `/ufm`) is the only way to release one.
